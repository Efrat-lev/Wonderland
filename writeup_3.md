
## Level 4 - 

**מטרה:** עקיפת מנגנון אימות מבוסס מצביעים (Pointers) ובדיקת "אנטי-רמאות" (Anti-Cheat), באמצעות חישוב אופסט למחרוזת חלופית השמורה במרחב הזיכרון.

עם המעבר לשלב 4, התוכנית הציגה את הודעת הפתיחה הבאה שדרשה קלט מספרי:

<div dir="ltr" align="left">

```text
Wait... I have something on the tip of my tongue!
(Enter the correct number)


```

</div>

חיפשתי את המחרוזת הזו בחלון ה-Strings ב-IDA וקפצתי אל פונקציית האימות `sub_401570`.

בתחילת הפונקציה ראיתי קריאה ל-`scanf` שקולטת מספר לתוך המשתנה המקומי `Str1`. לאחר מכן, זיהיתי סדרה של פקודות שמציבות ערכים לתוך המשתנה `Str` במחסנית. בין הפקודות זיהיתי שימוש בפקודת `shl` (Shift Left) לצורך פעולות מתמטיות, והכנסה של חלקים לתוך הזיכרון.
למעשה, התוכנית בונה כאן מחרוזת על המחסנית (טכניקת Stack String) בצורה חשאית. המחרוזת שנבנית היא `"!!Good luck"`.

לאחר בניית המחרוזת, התוכנית משתמשת בפונקציה `strncmp` כדי להשוות מחרוזות. הפרמטר המעניין כאן הוא הקלט שלנו (`Str1`), שמועבר לפונקציה ככתובת בזיכרון (Pointer), ולא כערך רגיל. התוכנית בעצם מבקשת שנספק לה כתובת בזיכרון שמכילה את המילים `"Good luck"`.

עם זאת, התגלתה כאן מלכודת: במידה ו-`strncmp` מחזירה שוויון (ההשוואה עברה בהצלחה), התוכנית מבצעת את פקודת ההשוואה `cmp [ebp+Str1], eax`.
פקודה זו בודקת האם הכתובת שהזנו זהה לכתובת של המחסנית שעליה התוכנית בנתה את המחרוזת. אם הכתובות זהות, התוכנית מזהה אותנו כרמאים ומדפיסה `"Cheater.. Try again another way."`. המטרה היא למצוא מחרוזת זהה במקום אחר בזיכרון!

**מציאת מחרוזת חלופית (Data Extraction):**
ערכתי חיפוש נוסף ב-Data Segment של התוכנית ומצאתי את המחרוזת הבאה השמורה בכתובת `00404738`:
`"Yeah! Good luck!! (and good job!)"`

כדי שהפונקציה `strncmp` תאמת את המחרוזת, המצביע חייב לנחות בדיוק על האות `'G'`. לכן, הוספתי אופסט (Offset) של 6 בתים לכתובת ההתחלתית כדי לדלג על התווים `"Yeah! "`.
חישוב הכתובת החדשה: `00404738 + 6 = 0040473E`.

**פסאודו-קוד של הלוגיקה:**

<div dir="ltr" align="left">

```text
stack_string = "Good luck!!"
user_pointer = input() // Read as an integer address

if (strncmp(user_pointer, stack_string) == 0):
    if (user_pointer == address_of(stack_string)):
        print("Cheater.. Try again another way.")
    else:
        print("Yeah! Good luck!! (and good job!)")
else:
    print("Wrong!")


```

</div>

**פיצוח:**
פונקציית ה-`scanf` מצפה לקבל את הקלט כמספר שלם בעשרוני (Decimal). לכן, המרתי את הכתובת ההקסדצימלית המחושבת `0040473E` לבסיס עשרוני.
`0040473E (HEX) = 4212542 (DEC)`

**תוצאה:** המספר המחושב הוא `4212542`. הזנת המספר בתוכנית עקפה את מנגנון האנטי-רמאות והחזירה הודעת ניצחון.

