## Level 5 -

**מטרה:** ניתוח פונקציית אימות המבוססת על מכונת מצבים (State Machine) וקריאת קובץ חיצוני, פיענוח מבנה זיכרון דו-ממדי מתוך מערך חד-ממדי, ובניית אלגוריתם ניווט (מבוך) לעקיפת השלב.

עם הרצת השלב החמישי והאחרון בתוכנית, התבקשתי להזין קלט. אולם, ניתוח של פונקציית הפתיחה ב-IDA העלה כי אופן קבלת הקלט השתנה לחלוטין משלבים קודמים.

במקום לעבד את המחרוזת שהזנתי ישירות כסיסמה, התוכנית העבירה את הקלט שלי אל ה-Windows API לפונקציה `CreateFileA`. המשמעות הייתה ברורה: הקלט שהתוכנית מצפה לו הוא **שם של קובץ**. במידה ופתיחת הקובץ הצליחה (הוחזר Handle חוקי), התוכנית קוראת מתוכו עד `400h` בתים (1024 Bytes) בעזרת `ReadFile` אל תוך חוצץ מקומי במחסנית (Buffer בכתובת `[ebp-808h]`), סוגרת את הגישה לקובץ עם `CloseHandle`, ואז מעבירה את מצביע החוצץ אל פונקציית ה"שופט" – `sub_401770`. ה"סיסמה" לשלב הזה היא תוכן הקובץ שאני צריכה לייצר.

נכנסתי לפונקציית ה"שופט" כדי להבין איזו לוגיקה מופעלת על תוכן הקובץ שלי.

**ניתוח פונקציית ה"שופט" (sub_401770):**

**1. איתור נקודת ההתחלה:**
בתחילת הפונקציה ראיתי קריאה לפונקציית הספרייה `strchr`. הפונקציה חיפשה את התו `4Fh` (שהוא `'O'` ב-ASCII) בתוך משתנה גלובלי השמור בזיכרון ב-Data Segment תחת השם `Str` (כתובת `00404020`).
`strchr` מחזירה מצביע למיקום המדויק שבו נמצאה האות `'O'` בתוך המחרוזת הזו. המצביע הזה נשמר במשתנה המקומי `[ebp+var_4]`. מיד הבנתי ש-`var_4` משמש כ"סמן מיקום" (Player Pointer) על גבי לוח משחק כלשהו שהוגדר מראש בזיכרון, ונקודת ההתחלה שלי היא האות `'O'`.

**2. לולאת הקריאה ותעלול ה-Jump Table:**
לאחר מכן, התוכנית נכנסת ללולאה שעוברת על תוכן הקובץ שלי תו-אחר-תו. בכל איטרציה, היא שולפת תו מהקובץ (באמצעות שימוש ב-`var_C` כאינדקס קריאה) אל האוגר `eax`.
כאן נחשפה לוגיקה מבריקה של מהדרים לייעול פקודות `switch-case`:
התוכנית מחסירה מהתו שנקרא את הערך `31h` (התו `'1'`), ושומרת את התוצאה ב-`[ebp+var_8]`. פעולה זו מנרמלת את התווים `'1', '2', '3', '4'` למספרים הנקיים `0, 1, 2, 3`.
מיד לאחר מכן מתבצעת בדיקת גבולות: `cmp [ebp+var_8], 3` ואחריה קפיצה `ja` למקרה דיפולטיבי של שגיאה במידה והמספר גדול מ-3.

אם הקלט תקין, מתבצעת פקודת הקפיצה:
`jmp ds:jpt_4017AE[ecx*4]`
פקודה זו משתמשת בערך המנורמל שב-`ecx` (שהועתק מ-`var_8`) כאינדקס ל**טבלת קפיצות (Jump Table)**. מכיוון שמדובר בארכיטקטורת 32-ביט שבה כל כתובת תופסת 4 בתים, המעבד מכפיל את האינדקס ב-4 כדי לדלג אל הכתובת הנכונה בטבלה ולזנק לבלוק הפקודות המתאים.

**3. פיענוח לוגיקת התנועה (הגריד הדו-ממדי):**
ניתחתי את 4 בלוקי הפעולות שאליהם הטבלה מפנה, והבנתי איך מתבצעת התנועה על המערך החד-ממדי שבזיכרון:

* **מקרה 0 (התו '1'):** `add [ebp+var_4], 1` – קידום הסמן בבית אחד (תנועה **ימינה**).
* **מקרה 1 (התו '2'):** `add [ebp+var_4], 10h` – קידום הסמן ב-16 בתים (תנועה **למטה**).
* **מקרה 2 (התו '3'):** `sub [ebp+var_4], 1` – חיסור בית אחד מהסמן (תנועה **שמאלה**).
* **מקרה 3 (התו '4'):** `sub [ebp+var_4], 10h` – חיסור 16 בתים מהסמן (תנועה **למעלה**).

הערך `10h` (16 בעשרוני) היווה רמז קריטי: למרות שהזיכרון רציף, הלוגיקה מתייחסת למחרוזת היעד כאל **מטריצה דו-ממדית (מבוך) שרוחב כל שורה בה הוא בדיוק 16 תווים**. תזוזה של 16 בתים קדימה או אחורה שקולה לירידה או עלייה של שורה באותה עמודה.

**4. חוקי הניווט ובדיקת הניצחון:**
לאחר ביצוע התנועה, התוכנית שולפת את התו שנמצא בכתובת החדשה של הסמן.
אם התו הוא `2Eh` (התו `'.'` - נקודה), הלולאה ממשיכה וקוראת את הפקודה הבאה מהקובץ.
אם זה לא נקודה, הלולאה נשברת ומתבצעת בדיקה סופית: `cmp eax, 58h` (התו `'X'`). אם הסמן נעצר בדיוק על `'X'`, המשתנה `[ebp+var_10]` (דגל הניצחון) מקבל את הערך `1`. כל תו אחר (כמו `'#'` המייצג קיר) יוביל לכישלון.

**אזור ה- Data Extraction והרכבת המבוך:**
ניגשתי ל-Data Segment אל הכתובת שעליה הצביע המשתנה `Str` (`00404020`). חילצתי את המחרוזת השלמה:
`###################O####...########.##...#.########.##.###.######` (ובסופה התו `X`).
בהתבסס על אופסט התנועה האנכית (`16`), שברתי את המחרוזת החד-ממדית לשורות של 16 תווים. המבוך נחשף במלואו:

```text
################
###O####...#####
###.##...#.#####
###.##.###.#####
###....###...X##
################

```

**פסאודו-קוד של לוגיקת השופט:**

```text
maze_ptr = find_char(maze_memory, 'O')
user_commands = read_file(input_filename)

for cmd in user_commands:
    normalized_cmd = cmd - '1'
    
    switch(normalized_cmd):
        case 0: maze_ptr += 1    // Right
        case 1: maze_ptr += 16   // Down
        case 2: maze_ptr -= 1    // Left
        case 3: maze_ptr -= 16   // Up
        default: return FAIL
        
    if (*maze_ptr == '.'):
        continue
    else:
        break

if (*maze_ptr == 'X'):
    return SUCCESS
else:
    return FAIL

```

**פיצוח:**
כל שנותר הוא לפתור את המבוך, מנקודת ההתחלה `'O'` אל היעד `'X'`, כאשר מותר לדרוך אך ורק על תווים מסוג `'.'`.
פירטתי את מסלול ההליכה, צעד אחר צעד, ותרגמתי אותו חזרה למקשי השליטה שגיליתי:

* תנועה למטה 3 פעמים: `2 2 2`
* תנועה ימינה 3 פעמים: `1 1 1`
* תנועה למעלה פעמיים: `4 4`
* תנועה ימינה פעמיים: `1 1`
* תנועה למעלה פעם אחת: `4`
* תנועה ימינה פעמיים: `1 1`
* תנועה למטה 3 פעמים: `2 2 2`
* תנועה ימינה 3 פעמים: `1 1 1`

המסלול השלם והרציף הוא: `2221114411411222111`.

**תוצאה:**
יצרתי קובץ טקסט חדש בשם `path.txt` בתוך ספריית העבודה של הקובץ `Wonderland.exe`, ושמרתי בתוכו את מחרוזת הניווט בלבד.
הרצתי את השלב, הזנתי את שם הקובץ כקלט, ואלגוריתם הניווט עקף את מנגנון המכונה וצלח את המבוך באופן מושלם.